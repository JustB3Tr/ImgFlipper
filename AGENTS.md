# AGENTS.md

## Cursor Cloud specific instructions

### Repository State

This repository ("ImgFlipper") is a greenfield project at the idea stage. As of initial setup, it contains only a `README.md` describing the concept: a tool that creates a single image flippable between front and back sides (for cards, credit cards, documents, etc.).

### Development Environment

- **No tech stack has been established yet** — there are no source files, package managers, build tools, or dependencies.
- **No services to run** — there is nothing to build, lint, test, or start.
- **No setup scripts or configuration** — no `package.json`, `Makefile`, `Dockerfile`, `docker-compose.yml`, or similar.

### For Future Agents

Once the project is scaffolded with a tech stack, update this section with:
- How to install dependencies (update script)
- How to run the dev server
- How to run lint and tests
- Any non-obvious caveats discovered during development
